# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
pyyaml>=6.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0

# Resource monitoring
psutil>=5.9.0
pynvml>=11.5.0  # NVIDIA GPU monitoring

# TTS Models (install separately)
# For Qwen3-TTS:
qwen-tts>=0.2.0

# For GLM-TTS: clone repo and install
# git clone https://github.com/zai-org/GLM-TTS.git
# pip install -r GLM-TTS/requirements.txt

# Optional: Flash Attention for better performance
# pip install flash-attn --no-build-isolation
